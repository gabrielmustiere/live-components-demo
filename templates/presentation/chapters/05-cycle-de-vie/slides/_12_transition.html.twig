{% import 'presentation/_macros.html.twig' as ui %}

<section data-slide-id="05-transition">
    {{ ui.slide_header('5.13', 'Message clé', 'Le cycle en une phrase') }}

    <blockquote class="verdict">
        <strong>Stateless serveur, stateful DOM.</strong>
        <br>
        À chaque cycle : re-instanciation, re-hydratation, action, re-render, re-sérialisation.
        <br>
        Les hooks (<code>PostHydrate</code>, <code>PreReRender</code>, <code>PreDehydrate</code>) sont vos points d'accroche.
    </blockquote>

    <p>
        On a vu <strong>le modèle</strong> (ch. 3–4) et <strong>le moteur</strong> (ch. 5).
        On respire une seconde : deux démos courtes, en live,
        pour voir tout ce vocabulaire s'exécuter dans un navigateur.
    </p>

    <p><small>→ Chapitre 6 · Démo live</small></p>
</section>
