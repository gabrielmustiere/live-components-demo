{% import 'presentation/_macros.html.twig' as ui %}

<section data-slide-id="03-intro">
    {{ ui.slide_header('Chapitre 3', 'Twig Components', 'Le socle') }}

    <ol>
        <li>Quelle est la <strong>structure</strong> d'un composant, fichier par fichier ?</li>
        <li>Comment fonctionnent les <strong>props</strong> — typage, valeurs par défaut ?</li>
        <li>Comment passe-t-on du <strong>contenu HTML</strong> à l'intérieur (slots) ?</li>
        <li>Comment interagir avec les <strong>services PHP</strong> et les méthodes calculées ?</li>
        <li>Quels sont les <strong>pièges</strong> à connaître en production ?</li>
    </ol>

    <blockquote class="verdict">
        ⚠️ Un Twig Component est <strong>statique</strong> — il rend une fois, côté serveur, c'est terminé.
        La réactivité arrive au chapitre 4.
    </blockquote>

    <p><small>↓ pour démarrer</small></p>
</section>
