{% import 'presentation/chapters/02-avant-symfony-ux/_macros.html.twig' as ui %}

<section data-slide-id="02-twig-extension-bilan">
    {{ ui.slide_header('2.4', 'Twig extension', 'Bilan') }}

    {{ ui.pros_cons(
        [
            '<strong>Logique PHP</strong> disponible (services injectables)',
            'Testabilité PHP standard',
            'Appel très court côté Twig',
        ],
        [
            'HTML en string PHP → illisible, <strong>XSS facile</strong>',
            'Pas de slots, pas de props typées côté Twig',
            'Pas d\'état (fonction pure)',
            'Explosion du namespace Twig global',
        ]
    ) }}

    {{ ui.legitimate_uses([
        '<strong>Helpers purs</strong> (format date, slug, label d\'enum)',
        '<em>Pas</em> des composants d\'UI',
    ]) }}
</section>
