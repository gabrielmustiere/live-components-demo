{% import 'presentation/chapters/02-avant-symfony-ux/_macros.html.twig' as ui %}

<section data-slide-id="02-stimulus-bilan">
    {{ ui.slide_header('2.6', 'Template + JS', 'Bilan') }}

    {{ ui.pros_cons(
        [
            '<strong>Vraie interactivité</strong> côté client',
            'Séparation markup / comportement',
        ],
        [
            '<strong>Double codebase</strong> (validation PHP + JS)',
            'État client ≠ état serveur → désynchronisation',
            'Un endpoint ad hoc par interaction',
            'Boilerplate à chaque nouveau comportement',
        ]
    ) }}

    {{ ui.legitimate_uses([
        'Comportements <strong>purement client</strong> (menu, toggle, clipboard)',
        'Rôle résiduel de Stimulus dans Symfony UX — recadré, pas disparu',
    ]) }}
</section>
