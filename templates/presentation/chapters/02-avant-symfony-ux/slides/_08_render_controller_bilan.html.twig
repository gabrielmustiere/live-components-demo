{% import 'presentation/chapters/02-avant-symfony-ux/_macros.html.twig' as ui %}

<section data-slide-id="02-render-controller-bilan">
    {{ ui.slide_header('2.3', 'render(controller(...))', 'Bilan') }}

    {{ ui.pros_cons(
        [
            '<strong>Vraie logique PHP</strong> (DI, services, repositories)',
            'Template séparé',
            'Cache indépendant possible via ESI',
        ],
        [
            '<strong>Sous-requête HTTP</strong> complète à chaque rendu',
            'Event subscribers rejoués (security, locale, firewall…)',
            'Coût qui <strong>explose</strong> avec 10–15 fragments par page',
            'DX médiocre : composant éclaté en contrôleur + template',
        ]
    ) }}

    {{ ui.legitimate_uses([
        'Fragments avec <strong>TTL différente</strong> de la page (ESI + Varnish)',
        'Chargement <strong>lazy</strong> côté client via <code>render_hinclude</code>',
    ]) }}
</section>
