{% import 'presentation/_macros.html.twig' as ui %}

<section data-slide-id="02-transition">
    {{ ui.slide_header('2.7', 'Message clé', 'Symfony UX = bridge backend ↔ frontend', true) }}

    <blockquote class="verdict">
        <strong>On garde la productivité Symfony.</strong>
        <br>
        On gagne le modèle composant.
        <br>
        On évite la complexité SPA quand elle n'est pas nécessaire.
    </blockquote>

    <p>
        La vision est posée. Attaquons la <strong>première brique</strong> :
        <br>
        une classe PHP + un template Twig = un composant réutilisable.
    </p>

    <p><small>→ Chapitre 3 · Twig Components, le socle</small></p>
</section>
