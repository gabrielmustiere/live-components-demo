{% import 'presentation/_macros.html.twig' as ui %}

<section data-slide-id="05-intro">
    {{ ui.slide_header('Chapitre 5', 'Démo live') }}

    <p>
        Deux démos courtes pour ancrer le concept.
    </p>

    <ol>
        <li><strong>Twig Component</strong> — <code>Alert</code>, puis <code>ProductCard</code> avec service injecté</li>
        <li><strong>Live Component</strong> — un compteur, puis une recherche avec <code>debounce</code></li>
    </ol>

    <blockquote class="verdict">
        Objectif : montrer le saut qualitatif entre statique et réactif.
    </blockquote></section>
