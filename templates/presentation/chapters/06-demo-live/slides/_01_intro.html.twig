{% import 'presentation/_macros.html.twig' as ui %}

<section data-slide-id="06-intro">
    {{ ui.slide_header('Chapitre 6', 'Démo live', 'Rendre le concept tangible') }}

    <p>
        Le <strong>modèle</strong> est posé (ch. 3–4), <strong>le moteur</strong> est compris (ch. 5).
        Avant d'attaquer les cas d'usage concrets, on <strong>ancre</strong> tout ça dans deux démos courtes.
    </p>

    <ol>
        <li>
            <strong>Twig Component</strong> — <code>Alert</code> puis <code>ProductCard</code> avec service injecté.
        </li>
        <li>
            <strong>Live Component</strong> — un compteur, puis une recherche live avec <code>debounce</code>.
        </li>
    </ol>

    <p>
        Deux démos, <strong>~3 minutes chacune</strong>. Objectif : montrer le <strong>saut qualitatif</strong>
        entre les deux mondes.
    </p>

    <p><small>↓ pour démarrer</small></p>
</section>
