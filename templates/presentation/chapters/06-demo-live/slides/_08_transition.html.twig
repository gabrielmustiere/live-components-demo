{% import 'presentation/_macros.html.twig' as ui %}

<section data-slide-id="06-transition">
    {{ ui.slide_header('6.7', 'Message clé', 'Du concept au réel en deux démos', true) }}

    <blockquote class="verdict">
        <strong>Le vocabulaire n'est plus abstrait.</strong>
        <br>
        <code>LiveProp</code>, <code>LiveAction</code>, <code>data-model</code>, morphdom : vous les avez vus s'exécuter.
    </blockquote>

    <p>
        Maintenant qu'on a le <strong>réflexe</strong>, passons aux <strong>cas d'usage concrets</strong> :
        <br>
        recherche, formulaires conditionnels, listes filtrables, quick actions —
        les patterns qu'on retrouve dans <strong>90 %</strong> des backoffices Symfony.
    </p>

    <p><small>→ Chapitre 7 · Cas d'usage concrets</small></p>
</section>
