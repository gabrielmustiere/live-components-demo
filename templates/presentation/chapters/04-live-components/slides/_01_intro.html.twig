{% import 'presentation/_macros.html.twig' as ui %}

<section data-slide-id="04-intro">
    {{ ui.slide_header('Chapitre 4', 'Live Components') }}

    <p>
        On ajoute la <strong>réactivité server-driven</strong> par-dessus Twig Components.
    </p>

    <ol>
        <li>Qu'est-ce qu'un Live Component, concrètement&nbsp;?</li>
        <li>Les briques : <code>LiveProp</code>, <code>LiveAction</code>, data binding</li>
        <li>Comment l'état circule sans écrire de JS</li>
    </ol>

    <blockquote class="verdict">
        Une <strong>surcouche</strong> de Twig Components, pas un remplacement. Opt-in, composant par composant.
    </blockquote></section>
